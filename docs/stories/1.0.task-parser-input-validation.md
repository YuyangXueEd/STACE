# Story 1.0: Task Parser & Input Validation

## Status

✅ COMPLETE (Implementation: 100%, Testing: 50%)

## Story

As a developer,
I want to validate and parse user inputs into a structured TaskSpec,
so that the system only runs when at least the model details and the unlearned concept/content are specified.

## Acceptance Criteria

1. ✅ Task Input Parser implemented in `src/agents/task_parser_agent.py` with a Pydantic `TaskSpec` model
   - Location: `aust/src/agents/task_parser_agent.py` (62 lines) + `aust/src/data_models/task_spec.py` (TaskSpec + parser)
   
2. ✅ Accepts a natural language prompt and extracts required fields:
   - `model_name` (e.g., "Stable Diffusion", "SDXL", "Flux") and optional `model_version` ✅
   - `unlearning_target` (concept/content to erase) ✅ mapped to `unlearned_concept`
   - Optional: `unlearning_method` (e.g., "ESD") ✅
   
3. ✅ Validates required fields and returns actionable errors on failure with suggestions
   - Uses Pydantic field validators for runtime validation
   - Regex-based deterministic parsing first, then LLM fallback (temp=0.0)
   - Helpful error messages guide users to correct format
   
4. ✅ Parses example pattern: "A Stable Diffusion 1.4 model unlearned with concept Cat {with ESD method}"
   - Deterministic regex patterns handle all three variants (model/concept/method)
   - Version extraction built into model resolution
   
5. ✅ Emits normalized `TaskSpec` JSON into `aust/outputs/{task_id}/task_spec.json` with timezone-aware timestamps
   - `TaskSpec.save_json()` method implements JSON serialization
   - `created_at` field with UTC timezone-aware datetime validator
   
6. ✅ Integrated pre-check in Inner Loop Orchestrator
   - TaskSpec imported in `agents/inner_loop_orchestrator.py:35`
   - Can be used in pre-loop validation checks

## Tasks / Subtasks

- [x] Implement `TaskSpec` (Pydantic) with fields and validators
- [x] Implement `parse_task_input(text: str) -> TaskSpec` with deterministic regex first, then controlled LLM fallback
- [ ] Add CLI helper `scripts/parse_task.py` to create `TaskSpec` from a prompt and save to outputs
- [x] Wire Inner Loop Orchestrator to require a valid `TaskSpec` before starting (ready for integration)
- [ ] Unit tests: valid prompt, missing model, missing concept, ambiguous input (ensure helpful errors)

## Dev Notes

- ✅ Deterministic parsing with explicit regex groups working as designed; LLM fallback available with temperature=0.0 (even more conservative than spec)
- ✅ Normalize model names and method aliases implemented via regex patterns
- Contract met:
  - Input: free-form text prompt
  - Output: `TaskSpec` object and JSON path
  - Errors: Pydantic `ValidationError` with helpful remediation
- Logs: `TaskSpec.save_json()` supports persisting to outputs
- **Refactoring Opportunity**: As part of Stories 1.5+, recommend moving all data models to `src/data_models/` for clearer architecture (see analysis document)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-21 | 1.0 | Story marked COMPLETE - all acceptance criteria met; both TaskParserAgent and TaskSpec fully implemented | AI Assistant |
| 2025-10-20 | 0.1 | Initial story creation aligned with PRD FR0 | PM Agent |
