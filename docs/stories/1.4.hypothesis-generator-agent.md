# Story 1.4: Hypothesis Generator Agent

## Status

Draft

## Story

**As a** researcher,
**I want** a Hypothesis Generator agent that proposes stress tests for data-based unlearning methods,
**so that** the system can autonomously generate testable vulnerability hypotheses.

## Acceptance Criteria

1. Hypothesis Generator agent implemented in `agents/hypothesis_generator.py`
2. Agent accepts context inputs: task type (data-based), past experiment results (empty for first iteration), feedback from evaluator
3. Agent generates hypothesis in structured format: attack method, target unlearning method, experiment parameters, expected outcome
4. Seed templates (3-5 known attack patterns: membership inference, model inversion, data extraction) are pre-loaded in `configs/seed_hypotheses.yaml`
5. For MVP, hypothesis generation uses seed templates + basic LLM variation (no RAG yet - deferred to Epic 2)
6. Output hypothesis is logged to `outputs/hypotheses/` with timestamp

## Tasks / Subtasks

- [ ] Implement Hypothesis Generator agent (AC: 1, 2, 3)
  - [ ] Create `agents/hypothesis_generator.py` extending base_agent.py
  - [ ] Define HypothesisContext input model (task_type, past_results, feedback)
  - [ ] Implement `generate_hypothesis(context: HypothesisContext) -> Hypothesis`
  - [ ] Implement `refine_hypothesis(hypothesis: Hypothesis, critic_feedback: str) -> Hypothesis`
  - [ ] Add type hints for all methods

- [ ] Create Hypothesis data model (AC: 3)
  - [ ] Define Hypothesis Pydantic model in `loop/models.py`
  - [ ] Fields: hypothesis_id, attack_type, description, rationale, target, expected_outcome, confidence_score, novelty_score, generated_at
  - [ ] Add validation: confidence_score and novelty_score between 0.0-1.0
  - [ ] Add UUID generation for hypothesis_id

- [ ] Create seed hypothesis templates (AC: 4)
  - [ ] Create `configs/tasks/data_based_unlearning.yaml`
  - [ ] Define 3-5 seed templates: membership inference, model inversion, data extraction
  - [ ] Each template includes: attack_type, description_template, target_template, parameters
  - [ ] Add template selection logic in agent

- [ ] Implement hypothesis generation logic (AC: 5)
  - [ ] Load seed templates from config
  - [ ] Select template based on iteration (round-robin or random)
  - [ ] Use LLM to add variation to template
  - [ ] Incorporate feedback from previous iteration if available
  - [ ] Set novelty_score based on template vs custom
  - [ ] Generate structured hypothesis output

- [ ] Add hypothesis logging (AC: 6)
  - [ ] Create output directory: `outputs/hypotheses/`
  - [ ] Log each hypothesis as JSON with timestamp
  - [ ] Filename format: `hypothesis_{task_id}_{iteration}.json`
  - [ ] Include full hypothesis object and generation metadata

- [ ] Create unit tests
  - [ ] Test hypothesis generation with empty context
  - [ ] Test hypothesis generation with feedback
  - [ ] Test seed template loading
  - [ ] Test hypothesis refinement with critic feedback
  - [ ] Mock OpenRouter API calls

## Dev Notes

### Relevant Source Tree

**Files to Create:**
- `agents/hypothesis_generator.py` - Hypothesis Generator Agent
- `loop/models.py` - Pydantic models (Hypothesis, HypothesisContext)
- `configs/tasks/data_based_unlearning.yaml` - Seed hypothesis templates
- `configs/prompts/hypothesis_generator_data_based.yaml` - Agent prompt config

### Data Models

From [architecture/data-models.md](../architecture/data-models.md):

**Hypothesis:**
```python
class Hypothesis(BaseModel):
    hypothesis_id: str
    attack_type: str  # "membership_inference", "model_inversion", "data_extraction", "concept_leakage"
    description: str
    rationale: str
    target: str
    expected_outcome: str
    confidence_score: float  # 0.0-1.0
    novelty_score: float  # 0.0-1.0
    generated_at: datetime
```

### Component Specifications

From [architecture/components.md](../architecture/components.md):

**Hypothesis Generator Agent:**
- Extends `agents/base_agent.py`
- Interfaces: `generate_hypothesis(context) -> Hypothesis`, `refine_hypothesis(hypothesis, feedback) -> Hypothesis`
- Uses prompt engineering with task-specific differentiation
- Includes seed template fallback for early iterations
- Scores hypotheses for novelty

### Coding Standards

- Type hints mandatory
- Use Pydantic for data validation
- Use pathlib.Path for file operations
- Use logging module, not print()
- All datetime objects timezone-aware

### Testing

- **Location:** `tests/unit/test_hypothesis_generator.py`
- Mock OpenRouter API calls
- Test all public methods
- Test edge cases: empty context, max iterations

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-16 | 0.1 | Initial story creation | Sarah (PO Agent) |

## Dev Agent Record

*This section will be populated by the development agent during implementation*

## QA Results

*This section will be populated by QA Agent after implementation*
